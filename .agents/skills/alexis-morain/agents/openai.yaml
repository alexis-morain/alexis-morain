interface:
  display_name: "Alexis Morain"
  short_description: "Repo-specific patterns and workflows for alexis-morain"
  default_prompt: "Use the alexis-morain repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true